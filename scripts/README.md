# Scripts

Place local automation here if needed.

Keep scripts generic and read private credentials from environment variables or an ignored local config file. Do not commit OAuth tokens, recordings, transcripts, logs, or calendar exports.
